name: CICD

on: 
    push:
        branches:
            - main
jobs:
    build:
        runs-on: ubuntu-latest           
        
        steps:
        - uses: actions/checkout@v2
        - name: build delivery service
          run: docker build -t delivery-service:latest -f backend/services/delivery-service/Dockerfile backend/services/delivery-service
